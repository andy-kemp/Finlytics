param(
    [string]$SubscriptionId,
    [string]$ResourceGroup = 'rg-finlytics-prod'
)

Write-Host 'This is a placeholder for the v2 production backup workflow.'
Write-Host "SubscriptionId: $SubscriptionId"
Write-Host "ResourceGroup: $ResourceGroup"
Write-Host 'Add the chosen backup/export command once the SQL approach is finalized.'
